// Renamed to behavior-decision-system.test.ts in Phase 0.2 (Decision/Planning split).
// All tests live there now.
import { describe, it } from "vitest";
describe("BehaviorSelectionSystem (moved)", () => {
  it.skip("tests moved to behavior-decision-system.test.ts", () => {});
});
